# +mkmake+ -- Everything after this line is automatically generated

EXECUTABLES = move_filenames_1
CFLAGS ?= -Wall -Wextra

all: $(EXECUTABLES)

clean:
	rm -f core *.o $(EXECUTABLES) a.out


move_filenames_1: move_filenames_1.cpp
	g++ $(CFLAGS) -o move_filenames_1 move_filenames_1.cpp
